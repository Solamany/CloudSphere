<?php require 'includes/auth.php';require_login();require 'config/database.php';$pdo=db();
$fields=['server_name', 'operating_system', 'cpu', 'ram', 'storage', 'ip_address', 'status'];
if(isset($_GET['delete'])){$pdo->prepare('DELETE FROM servers WHERE id=?')->execute([(int)$_GET['delete']]);header('Location: servers.php');exit;}
$edit=null;if(isset($_GET['edit'])){$q=$pdo->prepare('SELECT * FROM servers WHERE id=?');$q->execute([(int)$_GET['edit']]);$edit=$q->fetch(PDO::FETCH_ASSOC);}
if($_SERVER['REQUEST_METHOD']==='POST'){$vals=[];foreach($fields as $f)$vals[]=$_POST[$f]??'';if(!empty($_POST['id'])){$set=implode(',',array_map(fn($f)=>"$f=?", $fields));$pdo->prepare('UPDATE servers SET '+$set+' WHERE id=?')->execute(array_merge($vals,[(int)$_POST['id']]));}else{$cols=implode(',',$fields);$marks=rtrim(str_repeat('?,',count($fields)),',');$pdo->prepare('INSERT INTO servers ('+$cols+') VALUES ('+$marks+')')->execute($vals);}header('Location: servers.php');exit;}
$rows=$pdo->query('SELECT * FROM servers ORDER BY id DESC')->fetchAll(PDO::FETCH_ASSOC);include 'includes/header.php';include 'includes/sidebar.php';?>
<main><header class="dash-head"><div><p class="tag">MANAGEMENT</p><h1>Virtual Servers</h1></div></header>
<div class="two-col"><form class="card form" method="post"><h2><?= $edit?'Edit':'Add New' ?> Virtual Server</h2><input type="hidden" name="id" value="<?=$edit['id']??''?>"><?php foreach($fields as $f):?><label><?=ucwords(str_replace('_',' ',$f))?><input name="<?=$f?>" value="<?=htmlspecialchars($edit[$f]??'')?>" required></label><?php endforeach;?><button class="btn">Save Resource</button></form>
<div class="card table-wrap"><table><thead><tr><th>ID</th><?php foreach($fields as $f):?><th><?=ucwords(str_replace('_',' ',$f))?></th><?php endforeach;?><th>Actions</th></tr></thead><tbody><?php foreach($rows as $r):?><tr><td><?=$r['id']?></td><?php foreach($fields as $f):?><td><?=htmlspecialchars($r[$f])?></td><?php endforeach;?><td><a href="?edit=<?=$r['id']?>">Edit</a> · <a class="danger" onclick="return confirm('Delete this item?')" href="?delete=<?=$r['id']?>">Delete</a></td></tr><?php endforeach;?></tbody></table></div></div></main><?php include 'includes/footer.php'; ?>