<?php require 'includes/auth.php'; require 'config/database.php'; $err='';
if($_SERVER['REQUEST_METHOD']==='POST'){try{$s=db()->prepare('SELECT * FROM users WHERE email=? AND status="Active"');$s->execute([$_POST['email']]);$u=$s->fetch(PDO::FETCH_ASSOC);if($u && password_verify($_POST['password'],$u['password'])){$_SESSION['user']=$u;header('Location: dashboard.php');exit;}$err='Invalid email or password.';}catch(Exception $e){$err='Database connection error. Import the SQL file and configure database.php.';}}
include 'includes/header.php';?><div class="login-wrap"><form class="login card" method="post"><div class="logo">☁</div><h1>Welcome back</h1><p>Sign in to CloudSphere</p><?php if($err)echo "<div class='alert'>$err</div>";?><label>Email<input type="email" name="email" required></label><label>Password<input type="password" name="password" required></label><button class="btn">Login to Dashboard</button><small>Demo: admin@cloudsphere.com / admin123</small></form></div><?php include 'includes/footer.php'; ?>